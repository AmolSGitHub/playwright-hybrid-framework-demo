/**
 * Centralized timeout constants for the framework.
 * All wait times, page load, and element waits are defined here.
 * Change values in one place to apply globally.
 */

export const Timeouts = {
    PAGE_LOAD: 30000,       // 30 sec for full page load
    ELEMENT_WAIT: 10000,    // 10 sec for element visibility
    SHORT_WAIT: 3000,       // 3 sec for small waits
};

